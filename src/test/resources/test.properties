#The placeholders in this properties file will be replaced by the maven resource filter plugin.
#This converts project-relative paths to absolute paths.
#The absolute paths are necessary to call the javadoc command-line tool.
java.home=${java.home}
doclet.path=${project.build.outputDirectory}
source.path=${project.basedir}/src/test/java
package.name=springfox.javadoc.example
class.dir=${project.build.directory}/test-classes
#This file is generated by the maven dependency plugin.
#It contains the paths to all dependencies and is used to set up the classpath for the javadoc command-line tool.
classpath.file=${project.build.directory}/test-classes/.classpath
